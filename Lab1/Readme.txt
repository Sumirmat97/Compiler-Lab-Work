1. The Assignment 1 has the the function to determine if a string file is valid or not.
	A string is valid if it is among the following:
		if
		else
		for
		while
		
2. Assignment 2: to count the no of tokens in a c file named input.c


3. Assignment 3 has the ITOA function which converts an integer into a string
	It takes an input in integer and converts it to a string.

	
